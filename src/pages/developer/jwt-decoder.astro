---
import BaseLayout from '../../layouts/BaseLayout.astro';
import ToolShare from '../../components/ToolShare.astro';
import SimilarTools from '../../components/SimilarTools.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
import Breadcrumbs from '../../components/Breadcrumbs.astro';
import ToolFAQ from '../../components/ToolFAQ.astro';
import SearchOverlay from '../../components/SearchOverlay.astro';
import SaveResultButton from '../../components/SaveResultButton.astro';
const schema = {
  "@context": "https://schema.org",
  "@type": "WebApplication",
  "name": "JWT Token Decoder",
  "url": "https://solverspro.com/developer/jwt-decoder/",
  "applicationCategory": "DeveloperToolsApplication",
  "operatingSystem": "Any (web browser)",
  "browserRequirements": "Requires HTML5/JavaScript",
  "description": "Free online JWT token decoder to inspect header, payload claims, and signature data locally in your browser with zero server uploads.",
  "offers": {
    "@type": "Offer",
    "price": "0",
    "priceCurrency": "USD"
  },
  "provider": {
    "@type": "Organization",
    "name": "SolversPro",
    "url": "https://solverspro.com"
  }
};
---

<BaseLayout
  title="JWT Token Decoder | SolversPro"
  description="JWT token decoder to parse, inspect, and verify JSON Web Token headers, payload claims, and signatures locally with zero server uploads."
  canonical="https://solverspro.com/developer/jwt-decoder/"
  schema={schema}
>
  <Header />
  <SearchOverlay />

  <main class="flex-1 py-12">
    <div class="container mx-auto px-4 py-12 max-w-5xl">
      <Breadcrumbs items={[
        { label: 'Home', href: '/' },
        { label: 'Developer Tools', href: '/developer' },
        { label: 'JWT Token Decoder' },
      ]} />
      <div class="mb-10 mt-4">
        <h1 class="text-4xl font-bold text-white mb-4">JWT Token Decoder</h1>
        <p class="text-zinc-400 text-lg max-w-2xl">
          The SolversPro JWT Token Decoder is a free browser-based developer utility that parses and inspects JSON Web Token (JWT) headers, claims, and signatures client-side with zero server uploads.
        </p>
      </div>

      <div class="flex flex-col lg:flex-row gap-6 w-full min-h-[600px]">
        {/* Input Side */}
        <div class="flex-1 flex flex-col gap-4">
          <div class="flex justify-between items-center bg-[#0d0d14] px-4 py-3 border border-white/[0.06] rounded-t-xl border-b-0">
            <h2 class="text-sm font-semibold uppercase tracking-wider text-zinc-400">Encoded Token</h2>
          </div>
          <textarea
            id="input-token"
            class="flex-1 w-full p-4 bg-[#1a1a24] border border-white/[0.06] rounded-b-xl focus:ring-1 focus:ring-[#ff6b35] focus:outline-none font-mono text-sm text-zinc-300 resize-none break-all"
            placeholder="eyJh..."
            spellcheck="false"
          ></textarea>
        </div>

        {/* Decoded Side */}
        <div class="flex-1 flex flex-col gap-4">
          <div class="flex justify-between items-center bg-[#0d0d14] px-4 py-3 border border-white/[0.06] rounded-t-xl border-b-0">
            <h2 class="text-sm font-semibold uppercase tracking-wider text-zinc-400">Decoded Data</h2>
          </div>
          <div class="flex-1 w-full p-4 bg-[#1a1a24] border border-white/[0.06] rounded-b-xl overflow-y-auto space-y-4">
            
            <div id="error-msg" class="hidden p-3 bg-red-500/10 border border-red-500/30 rounded-lg text-red-400 text-sm font-mono items-center gap-2">
              <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" class="flex-shrink-0"><circle cx="12" cy="12" r="10"></circle><line x1="12" y1="8" x2="12" y2="12"></line><line x1="12" y1="16" x2="12.01" y2="16"></line></svg>
              <span>Invalid JWT format</span>
            </div>

            <div id="decoded-content" class="space-y-6">
              <div>
                <h3 class="text-xs font-semibold uppercase tracking-wider text-red-400 mb-2">Header (Algorithm & Token Type)</h3>
                <pre id="output-header" class="bg-[#0d0d14] p-3 rounded-lg border border-white/[0.05] font-mono text-sm text-zinc-300 whitespace-pre-wrap min-h-[80px]"></pre>
              </div>
              <div>
                <h3 class="text-xs font-semibold uppercase tracking-wider text-purple-400 mb-2">Payload (Data)</h3>
                <pre id="output-payload" class="bg-[#0d0d14] p-3 rounded-lg border border-white/[0.05] font-mono text-sm text-zinc-300 whitespace-pre-wrap min-h-[150px]"></pre>
              </div>
              <div>
                <h3 class="text-xs font-semibold uppercase tracking-wider text-[#2dd4bf] mb-2">Signature</h3>
                <pre id="output-signature" class="bg-[#0d0d14] p-3 rounded-lg border border-white/[0.05] font-mono text-sm text-zinc-300 whitespace-pre-wrap break-all min-h-[60px]"></pre>
              </div>
            </div>

            <div class="mt-6 flex justify-end">
              <SaveResultButton id="save-jwt-btn" />
            </div>

          </div>
        </div>
      </div>

      <!-- Citable Content: Standard JWT Claims Reference -->
      <div class="mt-16 w-full max-w-4xl mx-auto">
        <div class="mb-6">
          <h2 class="text-2xl font-bold text-white mb-2">Standard JWT Claims & Header Specifications (RFC 7519)</h2>
          <p class="text-zinc-400 text-sm">
            Registered JSON Web Token claim keys, header parameters, and standard data types.
          </p>
        </div>

        <div class="overflow-x-auto rounded-xl border border-white/[0.08] bg-white/[0.02]">
          <table class="w-full text-left border-collapse text-sm">
            <thead>
              <tr class="border-b border-white/[0.08] bg-white/[0.04] text-zinc-300 font-semibold">
                <th class="py-3 px-4">Claim / Header</th>
                <th class="py-3 px-4">JSON Key</th>
                <th class="py-3 px-4">Data Type</th>
                <th class="py-3 px-4">Specification Meaning</th>
              </tr>
            </thead>
            <tbody class="divide-y divide-white/[0.06] text-zinc-400">
              <tr class="hover:bg-white/[0.02] transition-colors">
                <td class="py-3 px-4 font-mono text-emerald-400 font-medium">Algorithm</td>
                <td class="py-3 px-4 font-mono text-zinc-300">alg</td>
                <td class="py-3 px-4">String (e.g. HS256, RS256)</td>
                <td class="py-3 px-4">Cryptographic algorithm used to generate the signature in the header</td>
              </tr>
              <tr class="hover:bg-white/[0.02] transition-colors">
                <td class="py-3 px-4 font-mono text-emerald-400 font-medium">Subject</td>
                <td class="py-3 px-4 font-mono text-zinc-300">sub</td>
                <td class="py-3 px-4">String</td>
                <td class="py-3 px-4">Unique principal identifier (user ID or client ID)</td>
              </tr>
              <tr class="hover:bg-white/[0.02] transition-colors">
                <td class="py-3 px-4 font-mono text-emerald-400 font-medium">Issuer</td>
                <td class="py-3 px-4 font-mono text-zinc-300">iss</td>
                <td class="py-3 px-4">String / URI</td>
                <td class="py-3 px-4">Identity provider or authorization server that issued the token</td>
              </tr>
              <tr class="hover:bg-white/[0.02] transition-colors">
                <td class="py-3 px-4 font-mono text-emerald-400 font-medium">Expiration Time</td>
                <td class="py-3 px-4 font-mono text-zinc-300">exp</td>
                <td class="py-3 px-4">NumericDate (Seconds)</td>
                <td class="py-3 px-4">Unix timestamp beyond which the token is invalid and expired</td>
              </tr>
              <tr class="hover:bg-white/[0.02] transition-colors">
                <td class="py-3 px-4 font-mono text-emerald-400 font-medium">Issued At</td>
                <td class="py-3 px-4 font-mono text-zinc-300">iat</td>
                <td class="py-3 px-4">NumericDate (Seconds)</td>
                <td class="py-3 px-4">Unix timestamp when the token was originally signed</td>
              </tr>
              <tr class="hover:bg-white/[0.02] transition-colors">
                <td class="py-3 px-4 font-mono text-emerald-400 font-medium">Audience</td>
                <td class="py-3 px-4 font-mono text-zinc-300">aud</td>
                <td class="py-3 px-4">String / String Array</td>
                <td class="py-3 px-4">Resource server or API intended to accept the token</td>
              </tr>
            </tbody>
          </table>
        </div>
      </div>

      <ToolFAQ faqs={[
        { question: "Is decoding a JWT safe?", answer: "Yes, decoding a JWT only reads the Base64Url encoded public information. It does not modify or validate the token. Since our tool runs entirely in your browser, your token never leaves your device." },
        { question: "Can I edit the JWT payload?", answer: "Currently, this tool is meant for decoding and inspecting existing tokens. If you edit the payload, the original signature will become invalid." },
        { question: "Why do I see 'Invalid JWT format'?", answer: "A valid JWT must have exactly three parts separated by dots (header.payload.signature) and must be properly Base64Url encoded." }
      ]} />

      <ToolShare title="JWT Token Decoder" url="https://solverspro.com/developer/jwt-decoder" />
      <SimilarTools category="developer" currentPath="/developer/jwt-decoder" />
    </div>
  </main>
  <Footer />
</BaseLayout>

<script>
  const inputToken = document.getElementById('input-token') as HTMLTextAreaElement;
  const errorMsg = document.getElementById('error-msg');
  const decodedContent = document.getElementById('decoded-content');
  const outputHeader = document.getElementById('output-header');
  const outputPayload = document.getElementById('output-payload');
  const outputSignature = document.getElementById('output-signature');

  function decodeJWT(token: string) {
    if (!token) {
      errorMsg?.classList.add('hidden');
      decodedContent?.classList.remove('opacity-50');
      if (outputHeader) outputHeader.textContent = '';
      if (outputPayload) outputPayload.textContent = '';
      if (outputSignature) outputSignature.textContent = '';
      return;
    }

    try {
      const parts = token.split('.');
      if (parts.length !== 3) throw new Error("Invalid JWT string");

      // base64Url decode
      const b64Decode = (str: string) => {
        let base64 = str.replace(/-/g, '+').replace(/_/g, '/');
        const pad = base64.length % 4;
        if (pad) {
          if (pad === 1) throw new Error('Invalid Base64Url string');
          base64 += new Array(5 - pad).join('=');
        }
        return JSON.parse(decodeURIComponent(atob(base64).split('').map(function(c) {
          return '%' + ('00' + c.charCodeAt(0).toString(16)).slice(-2);
        }).join('')));
      };

      const headerObj = b64Decode(parts[0]);
      const payloadObj = b64Decode(parts[1]);

      if (outputHeader) outputHeader.textContent = JSON.stringify(headerObj, null, 2);
      if (outputPayload) outputPayload.textContent = JSON.stringify(payloadObj, null, 2);
      if (outputSignature) outputSignature.textContent = parts[2];

      errorMsg?.classList.add('hidden');
      decodedContent?.classList.remove('opacity-50');
      
      const saveBtn = document.getElementById('save-jwt-btn');
      if (saveBtn) {
        saveBtn.classList.remove('hidden');
        saveBtn.onclick = () => {
          if (typeof window.saveToDashboard === 'function') {
            const success = window.saveToDashboard('JWT Token Decoder', '/developer/jwt-decoder', {
              type: 'key-value',
              data: {
                'Header': JSON.stringify(headerObj),
                'Payload': JSON.stringify(payloadObj)
              }
            });
            if (success) {
              const textEl = saveBtn.querySelector('.btn-text');
              const iconSave = saveBtn.querySelector('.icon-save');
              const iconCheck = saveBtn.querySelector('.icon-check');
              if (textEl) textEl.textContent = 'Saved!';
              if (iconSave) iconSave.classList.add('hidden');
              if (iconCheck) iconCheck.classList.remove('hidden');
              saveBtn.classList.add('bg-green-500/20', 'text-green-400', 'border-green-500/30');
              saveBtn.classList.remove('bg-white/[0.05]', 'text-zinc-300', 'border-white/[0.1]');
              setTimeout(() => {
                if (textEl) textEl.textContent = 'Save Result';
                if (iconSave) iconSave.classList.remove('hidden');
                if (iconCheck) iconCheck.classList.add('hidden');
                saveBtn.classList.remove('bg-green-500/20', 'text-green-400', 'border-green-500/30');
                saveBtn.classList.add('bg-white/[0.05]', 'text-zinc-300', 'border-white/[0.1]');
              }, 2000);
            }
          }
        };
      }
      
    } catch (e) {
      errorMsg?.classList.remove('hidden');
      errorMsg?.classList.add('flex');
      decodedContent?.classList.add('opacity-50');
      document.getElementById('save-jwt-btn')?.classList.add('hidden');
    }
  }

  inputToken?.addEventListener('input', (e) => {
    decodeJWT((e.target as HTMLTextAreaElement).value.trim());
  });
</script>
